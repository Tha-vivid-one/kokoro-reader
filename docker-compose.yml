services:
  kokoro-tts:
    build: ./server
    ports:
      - "8787:8787"
    env_file: ./server/.env
    volumes:
      - model-cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  model-cache:
